spring.application.name=medistock-backend

# ===============================
# PostgreSQL Database
# ===============================
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://dpg-d9chnseq1p3s73envkp0-a.oregon-postgres.render.com/medicine_h3o6?sslmode=require}
spring.datasource.username=${DATABASE_USERNAME:srit}
spring.datasource.password=${DATABASE_PASSWORD:UGFYJ7A9TOeFFQdBN5I4bIXZyj2hj7zx}
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# JPA / Hibernate
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# PostgreSQL Dialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ===============================
# Server
# ===============================
server.port=${PORT:8080}